apiVersion: 1

providers:
  - name: MediQueue
    orgId: 1
    folder: MediQueue
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
