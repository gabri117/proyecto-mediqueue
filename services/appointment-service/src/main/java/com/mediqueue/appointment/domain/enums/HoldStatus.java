package com.mediqueue.appointment.domain.enums;

/**
 * Enumeration representing the status of a temporary slot hold.
 */
public enum HoldStatus {
    ACTIVE,
    RELEASED,
    EXPIRED,
    CONSUMED
}
